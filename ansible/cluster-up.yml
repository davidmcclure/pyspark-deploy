---

- name: Create nodes
  hosts: localhost
  tasks:

    - terraform:
        project_path: terraform/spark-cluster
        state: present
        force_init: yes

    - meta: refresh_inventory

- name: Deploy Spark
  hosts: spark
  gather_facts: no
  become: yes
  roles:
    - spark-cluster
